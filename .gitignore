# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Data / model artifacts (keep the repo light) — anchored to repo root so the
# source package slimserve/data/ is NOT ignored.
*.safetensors
*.bin
*.gguf
/checkpoints/
/data/

# Benchmark outputs are committed selectively; ignore scratch plots
results/charts/*.tmp

# Secrets — never commit tokens/keys
.env
*.env
.env.*

# OS / editor
.DS_Store
.idea/
.vscode/
